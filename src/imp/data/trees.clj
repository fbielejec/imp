(ns imp.data.trees
  (:import org.apache.commons.io.FileUtils )
  (:import java.net.URL)
  (:import java.io.File)
  )



; TODO create url in /trees
(defn handle-upload
  [input]
   
  (println 
    input
    )
   
   )




;
;(def trees (atom ""))
;
;       URL url = new URL("https://www.google.com");
;            File destination = new File("google.html");
;
;            // Copy bytes from the URL to the destination file.
;            FileUtils.copyURLToFile(url, destination);
;  
;(defn list-trees []
;  
;  (let [url (new URL "http://localhost:8080/trees") file (new File url) ]
;    
;    file
;    
;    )
;  
;  
;  )
;
;
;(defn add-trees
;  [input]
;  (reset! trees input)
;  ;  input
;  )

